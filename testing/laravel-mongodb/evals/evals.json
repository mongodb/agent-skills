{
  "skill_name": "laravel-mongodb",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a Laravel Eloquent model for a `posts` MongoDB collection. The model should have fields: title, body, author_id (FK to users), published_at. Show the full model class with correct imports, casts, and any MongoDB-specific configuration needed.",
      "expected_output": "A Post model extending MongoDB\\Laravel\\Eloquent\\Model with the collection set via $table, strict types, and casts for author_id (string) and published_at (datetime).",
      "expectations": [
        "Model extends `MongoDB\\Laravel\\Eloquent\\Model`, not `Illuminate\\Database\\Eloquent\\Model`",
        "Uses `protected $table = 'posts'` and does NOT use `protected $collection`",
        "Declares `protected $casts` with `'author_id' => 'string'`",
        "Declares `protected $casts` with `'published_at' => 'datetime'`",
        "File begins with `declare(strict_types=1);`",
        "Imports `MongoDB\\Laravel\\Eloquent\\Model` (fully-qualified MongoDB model)"
      ]
    },
    {
      "id": 2,
      "prompt": "How do I get a list of all distinct genres from my Movie collection in Laravel MongoDB? I want a simple array of strings like `['Action', 'Comedy', 'Drama']`.",
      "expected_output": "Use `Movie::distinct()->pluck('genre')` (or an aggregation with $group/$addToSet) and explicitly warn that `distinct('field')->get()` returns model objects, not scalar values.",
      "expectations": [
        "Does NOT use `->distinct('genre')->get()` expecting an array of scalar strings",
        "Uses `->distinct()->pluck('genre')` OR a raw aggregation with `$group` / `$addToSet`",
        "Explicitly warns that `distinct('field')->get()` returns a Collection of model objects rather than scalar values",
        "Final result is described as an array of scalar strings (not objects/models)",
        "Does NOT rely on a PHP-side `->flatten()->unique()` post-processing chain as the recommended approach"
      ]
    },
    {
      "id": 3,
      "prompt": "How do I count the number of comments per post in Laravel with MongoDB?",
      "expected_output": "Use a raw aggregation pipeline with $lookup and $size executed via Post::raw(), and avoid Eloquent's withCount().",
      "expectations": [
        "Does NOT use `Post::withCount('comments')` or `withCount(...)` on a MongoDB model",
        "Uses a `$lookup` stage and a `$size` operator in an aggregation pipeline",
        "Calls the aggregation via `Post::raw(...)` or `$collection->aggregate(...)`",
        "Explicitly states that `withCount` is not supported with the Laravel MongoDB package"
      ]
    },
    {
      "id": 4,
      "prompt": "How do I get 5 random documents from my MongoDB collection in Laravel?",
      "expected_output": "Use a raw aggregation pipeline with a $sample stage of size 5 executed via Model::raw(), and avoid inRandomOrder().",
      "expectations": [
        "Does NOT use `->inRandomOrder()`",
        "Uses the `$sample` aggregation stage with `'size' => 5`",
        "Calls the aggregation via `Model::raw(...)` or `$collection->aggregate(...)`",
        "Explicitly states that `inRandomOrder()` is not supported with the Laravel MongoDB package"
      ]
    },
    {
      "id": 5,
      "prompt": "My `belongsTo` always returns null but the `user_id` field exists in the document. My Post model has `public function author(): BelongsTo { return $this->belongsTo(User::class, 'author_id'); }`. What is wrong?",
      "expected_output": "Identify the ObjectId/string type mismatch as root cause, and fix it by casting author_id to string on Post and setting $keyType = 'string' on User (when User is a MongoDB model).",
      "expectations": [
        "Identifies an ObjectId vs string type mismatch (or BSON ObjectId vs string) as the root cause",
        "Adds `protected $casts = ['author_id' => 'string']` (or equivalent) on the Post model",
        "Mentions `protected $keyType = 'string'` on the User model when User is a MongoDB model",
        "Does NOT recommend adding `$keyType = 'string'` on a SQL/MySQL User model as the fix",
        "Does NOT suggest the relationship method name is the cause of the null return"
      ]
    },
    {
      "id": 6,
      "prompt": "How do I debug a MongoDB query in Laravel? I want to see the generated query, similar to how `toSql()` works for MySQL.",
      "expected_output": "Explain that MongoDB queries are BSON/array documents (not SQL) and recommend ->dump() or ->dd() to inspect the generated pipeline or filter.",
      "expectations": [
        "Does NOT suggest `toSql()` or `toRawSql()` as a working option for MongoDB",
        "Recommends `->dump()` or `->dd()` on the query builder",
        "Explicitly states there is no SQL for MongoDB queries (they are BSON/array documents or pipelines)",
        "Does NOT recommend `DB::enableQueryLog()` combined with `toSql()` as the primary debugging method"
      ]
    },
    {
      "id": 7,
      "prompt": "How do I use `DatabaseTransactions` in Pest tests with MongoDB?",
      "expected_output": "Explicitly state that DatabaseTransactions and RefreshDatabase are not supported with MongoDB and recommend manually truncating collections in beforeEach.",
      "expectations": [
        "Explicitly states that `DatabaseTransactions` is NOT supported with MongoDB in Laravel",
        "Does NOT recommend `RefreshDatabase` as a working alternative",
        "Recommends a manual cleanup pattern in `beforeEach` using `deleteMany([])`, `truncate()`, or collection drop",
        "Does NOT recommend `DB::beginTransaction()` / `rollBack()` per test as a working pattern on standalone MongoDB",
        "Mentions that MongoDB transactions require a replica set (or notes the standalone limitation)"
      ]
    },
    {
      "id": 8,
      "prompt": "How do I configure MongoDB as a cache store with TTL expiration in Laravel?",
      "expected_output": "Configure the built-in `mongodb` cache driver in config/cache.php and create a migration with a TTL index on expires_at using $collection->expire('expires_at', 0).",
      "expectations": [
        "Does NOT create a custom cache store class (no custom `MongoDBStore` implementation)",
        "Configures the built-in `'driver' => 'mongodb'` store in `config/cache.php`",
        "Creates a TTL index on the `expires_at` field (NOT on a field called `expiration`)",
        "Uses `$collection->expire('expires_at', 0)` or equivalent TTL index with expireAfterSeconds 0",
        "Does NOT create a unique index on `key` (the cache key is stored as `_id`)"
      ]
    },
    {
      "id": 9,
      "prompt": "How do I filter products by price range using Laravel Scout with MongoDB? I want to search for 'headphones' and filter to only show products where price is between 50 and 200.",
      "expected_output": "Use a raw aggregation pipeline combining $search for the text query with a $match stage for the price range, and note that Scout where() only supports equality on the MongoDB engine.",
      "expectations": [
        "Does NOT use chained `->where('price', '>=', N)` / `->where('price', '<=', N)` on a Scout `search()` call as a working solution",
        "Uses a raw aggregation pipeline with an Atlas `$search` stage followed by a `$match` stage for the price range",
        "Explicitly states that Scout `where()` on the MongoDB engine supports only equality filters (no range/comparison operators)",
        "Mentions that Atlas Search or raw aggregation is required for range filtering"
      ]
    },
    {
      "id": 10,
      "prompt": "I have Users in MySQL and Posts in MongoDB, how do I define the Eloquent relationship between them?",
      "expected_output": "Add the HybridRelations trait to the SQL User model, define HasMany/BelongsTo as usual, and cast the user_id FK to string on the MongoDB Post model.",
      "expectations": [
        "Adds `use HybridRelations` from `MongoDB\\Laravel\\Eloquent\\HybridRelations` on the SQL-side User model",
        "MongoDB Post model casts the foreign key (`user_id`) to `'string'`",
        "Does NOT add `protected $keyType = 'string'` on the SQL User model",
        "Does NOT add the `HybridRelations` trait to the MongoDB Post model (only to the SQL side)",
        "Indicates the SQL model stores or relates the MongoDB `_id` as a string value"
      ]
    }
  ]
}
